const Button = () => {
    return(
        <button className="btn btn--liquidBtn">
            <span>Get in touch</span>
            <div className="btn--liquidBtn--liquid"/>
        </button>
    )
}

export default Button